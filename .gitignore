# Garmin Connect IQ / Monkey C Build Outputs
bin/
gen/
mir/
internal-mir/
*.prg
*.iq
*.debug
*.xml.debug
*.prg.debug.xml

# VS Code Connect IQ Extension
.vscode/project.json
*.json.debug

# Eclipse / Monkey C IDE Project Files
.project
.classpath
.settings/

# IntelliJ / Android Studio (if used for editing)
.idea/
*.iml

# System & OS Files
.DS_Store
Thumbs.db
ehthumbs.db

# Temporary Files & Logs
*.log
*.tmp
*.bak

# Claude Code local settings (contains secrets, e.g. LiteLLM key)
.claude/settings.local.json

# Machine-local build config (SDK path, signing key) — see .build.local.env.example
.build.local.env

# Connect IQ developer signing key — never commit, it's a secret
developer_key
